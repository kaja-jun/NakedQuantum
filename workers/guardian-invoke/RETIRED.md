# Guardian auto-invoke worker — retired (May 2026)

The Soup **auto-invoke strip** and Cloudflare Worker (`naked-guardian`) were removed from the PWA.

**Replaced by:** always-on witness substrate (`nq_synapse_latest`, bridges, ◇ SUBSTRATE panel) + **voluntary Guardian summon** (BYOK OpenRouter).

Historical `guardian_logs` rows with `log_type: auto_invoke` remain in the vault for audit.
